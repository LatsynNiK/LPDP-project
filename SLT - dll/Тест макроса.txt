модель тест
блок контроллер ГЕН
	описание
		Генерировать := 
		алгоритм
		начало:	создать объект Задача типа вектор (s1,s2 -- скаляр );
			Задача(s1) := ЦЕЛОЕ(RAND*5)+1;
			Задача(s2) := ВРЕМЯ;
			создать объект S типа ссылка;
			S := ссылка на Задача;
			активизировать инициатор из S направить инициатор на метку Нач блока ОБРАБОТКА;
			ВремяГен := ВРЕМЯ-(ln(RAND)/L);
			ждать ВРЕМЯ=ВремяГен;
			направить инициатор на метку начало;
		все алгоритм -- макрос(МатОж, Дисп);
		ВремяГен -- скаляр;
		L:=0.02 -- скаляр;	 // Интенсивность потока		
	все описание
	алгоритм
начало:	создать объект Задача типа вектор (s1,s2 -- скаляр );
		Задача(s1) := ЦЕЛОЕ(RAND*5)+1;
		Задача(s2) := ВРЕМЯ;
		создать объект S типа ссылка;
		S := ссылка на Задача;
		активизировать инициатор из S направить инициатор на метку Нач блока ОБРАБОТКА;
		ВремяГен := ВРЕМЯ-(ln(RAND)/L);
		ждать ВРЕМЯ=ВремяГен;
		направить инициатор на метку начало;
	все алгоритм
все блок
все модель